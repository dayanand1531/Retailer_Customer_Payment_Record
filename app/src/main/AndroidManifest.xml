<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.generalstore">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity"></activity>
        <activity android:name=".Customer_Sale_Report" />
        <activity android:name=".Customer_buy_repoat" />
        <activity android:name=".SignUp" />
        <activity android:name=".Login" />
        <activity android:name=".Change_password" />
        <activity android:name=".Product_Sale_Report" />
        <activity android:name=".Payment_Report" />
        <activity android:name=".UserReport" />
        <activity android:name=".Payment" />
        <activity android:name=".Customer_Product_Order" />
        <activity android:name=".AddNewUser" />
        <activity android:name=".Splesh">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>